#pragma once

#include <iostream>
#include <unistd.h>
#include <string.h>
#include <sys/socket.h>
#include <arpa/inet.h>

#define PORT 8000

typedef struct sockaddr_in SA_IN;
typedef struct sockaddr SA;
